# Arduino-LCD-Calculator
An Arduino based Calculator which uses a 16X2 LCD Screen, the Arduino Serial monitor and an Arduino UNO. Two numbers are taken from the user, along with the operator. The calculation is complete and the output is visible in the Arduino Serial Monitor and on the LCD Screen.
